# Unity README Baseline

## Goal

Keep the generated README aligned with the repository's Unity source baseline and verification limits.

## Scope

- Document the SDK-free source baseline command.
- Preserve the legacy Unity 5.6.1p1 toolchain note.
- Keep the SodaSpawn 1000-can cleanup cap visible to future maintainers.
- Avoid changing Unity scenes, prefabs, or C# behavior.

## Verification

- `scripts/check-baseline.sh`
